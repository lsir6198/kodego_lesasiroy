package com.oop2

abstract class Animal {

        abstract fun walk()
        abstract fun makeSound()

}

    //it can have abstract functions => are functions without a body/implementation
    //only abstract classes can have abstract function
    //cannot create an object from an abstract class
    //abstract is always a superclass only
    //when you inherit from an abstract class, you are required to override all its abstract functions
