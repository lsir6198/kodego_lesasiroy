package com.oop3

// not a class, no constructors
//cannot create an object
//can have abstract functions without abstract keyword
//being implemented rather than inherited

interface Animal {
    fun walk()
    fun makeSound()
}